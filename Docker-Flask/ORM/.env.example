# Copia este archivo como ".env" (mismo directorio) y ajusta el valor.
# ".env" NO se sube al repositorio (ver .gitignore).

# Llave secreta usada para firmar los tokens JWT de sesión.
# Usa una cadena larga y aleatoria en un entorno real, por ejemplo:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambia-esto-por-una-llave-larga-y-aleatoria
